name: Unit Tests

on:
  pull_request:
  push:
    branches:
      - main
      - devel

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run tests
        run: ./run_tests.sh
